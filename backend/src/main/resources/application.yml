spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eventos_db
    username: eventos_user
    password: eventos_pass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true